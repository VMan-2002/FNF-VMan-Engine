this will not the only way to implement your update checks, so don't pull request to add your mod to this list!!!

if your bumping a version number here, do NOTHING ELSE in the same commit

(also at some point this'll just be moved to another repository so uh yeah lol)